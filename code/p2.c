// #include<stdio.h>
// int main()
// {
//     //int a;
//     //short int a;
//     //long int a;
//     //char a; 
//     //float a;
//     //double a;
//     //long double a;
//     printf("%d",sizeof(a));
//     return 0;
// }

// printf("<jo terminal me aayega>",agar "" ke andar kisi format specifier ka use kiye ho toh udhar jo value jayega)
// int a=5;
//printf("%d",a);

// scanf("format specifier", address ya &variablename);

// if(condition) {
//     statements
// } else if(condition) {
//     statements
// } else {
//     statements
// }

// arithmetic operators-> +,-,/,*,%

// % -> used to find remainder

// >,<,>=,<=,==,!= -> conditions
// multiple conditions -> logical operators
// logical operators-> &&(Logical AND), ||(Logical OR)

// to check if a is divisible by b (jab a ko b se divide karenge tab remainder 0 aana chahiye)
// a%b==0

// equal to (==) , assignment(=), not equal to(!=)

// simulataneously 2 conditions check karne hai (dono true hone chahiye) -> &&
// example: ek variable a h. print "Yes" if it is divisible by both 5 and 6 else print "No"

// jab 2 conditions me koi ek true ho -> ||
// example: ek variable a h. print "Yes" if it is divisible by either 5 or 6 else print "No"

/*
    odd-even: 
    even: a%2==0
    odd: a%2==1 ya a%2!=0    
*/

//conditional operator/statements -> alternative to if else

// condition ? statements to be executed if condition is true : statements to be executed if condition is false

// Typecasting
// convert one datatype to another

// datatypes-> int,float,char;

// int a;
// (float) a;

// (datatype to be converted to) variable name;

// division me agar float value chahiye toh denominator ko float me typecast kardo

// numerator/denominator -> integer
// numerator/(float)denominator -> float value

// ASCII (American Standard Code For Information Interchange)

// 48 to 57 (numbers) 0 to 9
// 65 to 90 (uppercase characters) A to Z
// 97 to 122 (lowercase characters) a to z
// rest are symbols

// A to Z (65 to 90)
// a to z (97 to 122)

// A to a (65+32)
// a to A (97-32)

// Z to z (90+32)
// z to Z (122-32)

/*
switch(expression) {  //if
    case value1:      //condition 1
        statements
        break;
    case value2:      // else if condition 2
        statements
        break;
    case value3:      // else if condition 3
        statements
        break;
    default:         // else (default case me break ki zarurt nhi hai aur default case hona bhi zaruri nhi h)
        statements
}
*/

// 0: false
// 1: true


